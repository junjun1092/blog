今天出现了一个问题，查询排班数据的时候报错，google控制台显示404，eclipse控制台显示null object，报错的堆栈信息都打印出来了，但是还是找了好久；
最后发现是数据库数据没有更新，显示的days是七天的数据，到2016-08-01号，但是schedule表中的数据没有到8月1号的，所以导致赋值出错；
代码里边对于问题的规避措施很少，以后的任务比较艰巨；
在前置机上没有更新代码，导致我的预约查询不出结果，以后这种代码上的低级问题还是要少出，一定要仔细；

bee工具的安装：
1 go get github.com/beego/bee,下载并安装bee工具到$GOPATH/bin目录下
在$GOPATH/bin目录下执行bee命令是可以，但是在其他的文件夹下执行就不能识别，明天继续操作
    
